'use strict';

describe('APIM.policytr module', function() {

  beforeEach(module('APIM.policytr'));

  describe('policytr controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var policytr_ctrl = $controller('policytr_ctrl');
      expect(policytr_ctrl).toBeDefined();
    }));

  });
});