'use strict';

describe('APIM.api module', function() {

  beforeEach(module('APIM.api'));

  describe('api controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var api_ctrl = $controller('api_ctrl');
      expect(api_ctrl).toBeDefined();
    }));

  });
});