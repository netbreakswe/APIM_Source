'use strict';

describe('APIM.policytp module', function() {

  beforeEach(module('APIM.policytp'));

  describe('policytp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var policytp_ctrl = $controller('policytp_ctrl');
      expect(policytp_ctrl).toBeDefined();
    }));

  });
});