'use strict';

describe('APIM.conto module', function() {

  beforeEach(module('APIM.conto'));

  describe('conto controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var conto_ctrl = $controller('conto_ctrl');
      expect(conto_ctrl).toBeDefined();
    }));

  });
});