'use strict';

describe('APIM.reset_password module', function() {

  beforeEach(module('APIM.reset_password'));

  describe('reset_password controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var reset_password_ctrl = $controller('reset_password_ctrl');
      expect(reset_password_ctrl).toBeDefined();
    }));

  });
});