'use strict';

describe('APIM.lista_api module', function() {

  beforeEach(module('APIM.lista_api'));

  describe('lista_api controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var lista_api_ctrl = $controller('lista_api_ctrl');
      expect(lista_api_ctrl).toBeDefined();
    }));

  });
});