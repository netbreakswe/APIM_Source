'use strict';

describe('APIM.account module', function() {

  beforeEach(module('APIM.account'));

  describe('account controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var account_ctrl = $controller('account_ctrl');
      expect(account_ctrl).toBeDefined();
    }));

  });
});