'use strict';

describe('APIM.registra_utente module', function() {

  beforeEach(module('APIM.registra_utente'));

  describe('registra_utente controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registra_utente_ctrl = $controller('registra_utente_ctrl');
      expect(registra_utente_ctrl).toBeDefined();
    }));

  });
});