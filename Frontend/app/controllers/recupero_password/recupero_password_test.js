'use strict';

describe('APIM.recupera_password module', function() {

  beforeEach(module('APIM.recupera_password'));

  describe('recupera_password controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var recupera_password_ctrl = $controller('ecupera_password_ctrl');
      expect(ecupera_password_ctrl).toBeDefined();
    }));

  });
});