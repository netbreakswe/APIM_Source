'use strict';

describe('APIM.modifica_interf_api module', function() {

  beforeEach(module('APIM.modifica_interf_api'));

  describe('modifica_interf_api controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var modifica_interf_api_ctrl = $controller('modifica_interf_api_ctrl');
      expect(modifica_interf_api_ctrl).toBeDefined();
    }));

  });
});