'use strict';

describe('APIM.registra_api module', function() {

  beforeEach(module('APIM.registra_api'));

  describe('registra_api controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registra_api_ctrl = $controller('registra_api_ctrl');
      expect(registra_api_ctrl).toBeDefined();
    }));

  });
});