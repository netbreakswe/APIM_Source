'use strict';

describe('APIM.lista_transazioni module', function() {

  beforeEach(module('APIM.lista_transazioni'));

  describe('lista_transazioni controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var lista_transazioni_ctrl = $controller('lista_transazioni_ctrl');
      expect(lista_transazioni_ctrl).toBeDefined();
    }));

  });
});