*Legenda*
Prima c'è il titolo che indica la sezione di cui parlo.
Poi la descrizione dello stato.
Il simbolo "-" indica mancanze gravi
Il simbolo "+" indica extra molto utili

Generale:

- Impostare il rerouting nel caso uno sviluppatore entri in zone riservate a developer (es. digitando manualmente l'indirizzo)
- Limitare l'accesso diretto a zone riservate (es. registrazione nuova api da restringere a developer su ogni pagina html)

+ Da inserire delle pagine di guida, perlomeno per il download ed utilizza di una api acquistata? Magari regalando free un servizio hello per far capire il meccanismo senza che uno paghi e poi non sappia effettuare le chiamate

Sessioni:
Ok.

Homepage:
Quasi.

+ La ricerca per keywords agisce anche su campi inutili (idMS,Name,IdDeveloper,Logo,Categorie): da raffinare tramite js
+ Migliorare la grafica del box ricerca

Login:
Quasi.

- Controllare la gestione errori

+ Decidere se le info developer-only rimangono tali, oppure se possono venire impostate anche da utenti basic (aboutme, cittadinanza, linktoself, paypal)

Homepage loggati:
Ok.

Pagina singola API:
Ok (compreso il download) e la mancanza dell'acquisto.

- Mancanza di qualsiasi tipo di procedura d'acquisto
- Controllare correttezza

+ Mettere un pulsante copia rapida  vicino all'identificativo? (magari stessa cosa nel profilo utente)

Pagina singola Policy:
Quasi.

- Da controllare correttezza del testo e formula

Pagina singolo Utente:
Ok.

Gestione Account:
Quasi.

- Le immagini vengono salvate sul server appena si apre la preview e non al salvataggio, provocando affollamento. Accade in ogni inserimento di immagine (reg utenti/api)
- Le immagini non vengono cancellate se le si cambia. Accade in ogni inserimento di immagine (reg utenti/api)
- Controllare la gestione errori

+ Manca la preview della nuova immagine
+ In realtà valgono altri formati oltre a png, jpeg, gif tipo btm

API Registrate:
Ok.

API Acquistate:
Ok.

Upgrade account:
Ok.

Registra API:
Funziona, compresa la parte backend e l'utilizzo immediato dell'API.

- Correggere testo ed abbellire tutto che è un po' confusionario
- Nella scelta categorie, ng-repeat crea un button inutile che cliccato scompare, da sistemare
- Controllare la gestione errori

Conferma registrazione API:
Ok.

+ Da personalizzare, tipo mettere un recap del servizio inserito

Modifica API:
Non lo so, non l'ho guardato. Comunque occorre mettere un link ad esso, tipo nel caso tu sia il developer puoi accedere alla pagina di modifica dalla schermata di visualizzazione dell'API (c'è già la funzione per vedere se sei il developer) ed inserirlo nella lista a sinistra della tua "area personale", sotto api registrate o dentro, dove vi pare.

Registrazione utente:
Quasi.

- Controllare la gestione errori

Recupero password:
Da implementare.

Ricarica crediti:
Quasi.

- Da collegare a PayPal le varie icone ed effettuare la vera gestione dell'acquisto e dell'attribuzione dei crediti

Registro transazioni:
Insomma.

- Da implementare anche i pagamenti al developer ed integrarli in view e controller
- Da mettere il servizio al posto della APIKey come valore nella tabella

Parte Admin:
Da implementare.

